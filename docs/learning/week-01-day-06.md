# CareerLens Week 1 Day 6 学习计划

> 本文档保存 Week 1 Day 6 的计划、进度与验收情况。当天完成后仍保留，到 Week 1 周复盘完成后再统一清理。

## 当前状态

- 周次：Week 1
- 学习日：Day 6
- 主题：整合、重构、README 与 Git
- 状态：已完成
- 计划时长：约 4 小时

## 当日目标

完成后应当能够：

- 用一份 200–500 字的脱敏 JD 验证完整命令行流程。
- 从用户角度检查输入、处理、输出和错误路径。
- 在测试保护下进行小范围代码整理，并区分重构与新增功能。
- 把 README 从单纯学习记录补充成别人可以使用的项目说明。
- 理解工作区、暂存区和提交记录的基本关系。
- 使用 `.gitignore` 排除虚拟环境、缓存、生成结果和临时隐私材料。
- 查看实际差异，只提交本次确认过且安全的文件。
- 创建一个语义清楚、可以复现的 Week 1 项目提交。

## 开始时的真实状态

- Git 仓库根目录：`~/projects/careerlens-learning/`。
- 当前分支：`master`。
- 已有提交：`docs: add AI agent learning roadmap and week one plan`。
- 当前项目目录和 `docs/learning/` 尚未被 Git 跟踪。
- `data/raw/sample_jd.txt` 当前约 124 个字符，尚未达到 200–500 字整合样本要求。
- 已生成 `.venv/`、`.pytest_cache/` 和多个 `__pycache__/`。
- `tmp/pdfs/resume/page-1.png` 可能包含个人材料：保留在本机，但不得进入提交。

## 今天的整体数据流

```text
200–500 字脱敏原始 JD
→ CLI 读取 UTF-8 文件
→ clean_jd() 清洗字符串
→ CLI 创建输出目录并写入结果
→ pytest 验证清洗规则
→ README 记录复现步骤
→ Git 只保存源码、测试、文档和安全样本
```

## 阶段 1：现状诊断与 Git 基础（约 30 分钟）

- [x] 确认 Git 仓库根目录、当前分支和已有提交。
- [x] 查看当前未跟踪内容及生成目录。
- [x] 检查样本长度与临时目录，识别潜在隐私风险。
- [x] 理解工作区、暂存区和提交记录的区别。
- [x] 理解 `git status`、`git diff`、`git add` 和 `git commit` 各自解决什么问题。
- [x] 理解“未跟踪”不等于“已经提交”。

## 阶段 2：确认提交边界与 `.gitignore`（约 35 分钟）

创建文件前共同决定 `.gitignore` 应位于仓库根目录，并逐条理解候选规则：

```text
careerlens/.venv/
**/__pycache__/
*.py[cod]
careerlens/.pytest_cache/
careerlens/data/processed/
tmp/
```

- [x] 理解 `.gitignore` 只影响未跟踪文件，不负责删除本机文件。
- [x] 区分源码、依赖清单、原始脱敏样本、生成结果和本机缓存。
- [x] 确认 `tmp/` 与任何个人简历材料不进入版本控制。
- [x] 由学习者创建并逐条填写根目录 `.gitignore`。
- [x] 用 `git status --short --ignored` 验证规则生效。

## 阶段 3：准备整合样本（约 30 分钟）

- [x] 确定脱敏原则：不含真实姓名、电话、邮箱、公司内部信息或可识别个人经历。
- [x] 将 `data/raw/sample_jd.txt` 扩充为 200–500 字。
- [x] 有意保留连续空格、Tab、首尾空白和空行，作为清洗输入。
- [x] 确认文本内容适合进入公开 Git 历史。

样本文本属于练习数据，不是核心 Python 代码；可由导师先给提案，学习者理解、检查并批准后写入。

## 阶段 4：完整流程与代码审查（约 45 分钟）

- [x] 删除旧的处理结果，并依据命令重新生成。
- [x] 对比原始文本与清洗结果，确认支持的规则。
- [x] 验证不存在输入文件时仍返回非零退出码。
- [x] 运行 8 个 pytest 测试并确认全部通过。
- [x] 检查 `cleaner.py`、`cli.py` 是否有重复逻辑、无用函数或职责混杂。
- [x] 只做能解释且有测试保护的小范围排版或命名整理。
- [x] 每项重构前先说明原因，由学习者决定并亲手修改。

## 阶段 5：把 README 补成项目说明（约 55 分钟）

保留 Day 1–Day 6 学习记录，同时在前面补充面向使用者的内容：

- [x] 项目当前解决什么问题。
- [x] 当前目录结构及核心文件职责。
- [x] Python 版本与环境安装命令。
- [x] CLI 运行命令。
- [x] pytest 测试命令。
- [x] 当前支持的清洗规则。
- [x] 当前不支持的能力：LLM 提取、数据库、RAG 和 Agent。
- [x] 输入数据必须脱敏的提醒。

README 内容由学习者先理解并确认；可以由导师提供文档草案，因为它是使用说明而非待掌握的核心 Python 实现。

## 阶段 6：Git 暂存、审查与提交（约 50 分钟）

- [x] 用 `git status --short` 区分未跟踪与已修改文件。
- [x] 使用明确路径 `git add`，不在未检查时直接加入全部内容。
- [x] 用 `git diff --cached --stat` 查看暂存范围。
- [x] 用 `git diff --cached` 抽查实际内容和隐私。
- [x] 确认 `.venv/`、缓存、生成输出和 `tmp/` 没有被暂存。
- [x] 运行最终 CLI 和测试。
- [x] 创建语义清楚的提交：`feat: add initial JD text cleaner`。
- [x] 查看提交摘要并理解提交保存了什么。

提交是本地 Git 历史变更，不会自动上传 GitHub；今天不进行远程推送。

## 阶段 7：Day 6 验收（约 15 分钟）

- [x] 删除 `data/processed/sample_jd.txt` 后，能只根据 README 重建。
- [x] `PYTHONPATH=src python -m pytest -v` 全部通过。
- [x] `git status` 没有遗漏的应提交变更，并且只剩预期忽略内容或完全干净。
- [x] 能口述从原始 JD 到处理文件的完整数据流。
- [x] 能解释 `.gitignore`、暂存区和 commit 的职责。
- [x] 能说明为什么 `.venv/`、缓存、生成结果和个人材料不应提交。
- [x] 由导师根据真实对话、命令、错误和修改写入 Day 6 学习记录。

## 当天完成后的保留规则

1. 把学习总计划中 Day 6 的状态改为“已完成”。
2. 将本文档状态改为“已完成”，保留计划和勾选记录。
3. 开始 Day 7 时新建 `week-01-day-07.md`，不覆盖本文档。
4. Week 1 复盘完成并保存周总结后，再统一清理本周的每日计划文档。
